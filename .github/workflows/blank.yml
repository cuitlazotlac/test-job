name: CI

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]
  workflow_dispatch:

jobs:
  fake-run:
    runs-on: tenki-standard-small-2c-4g
    steps:
      - name: Wait 10 minutes
        run: sleep 3600  # 600 seconds = 10 minutes

      - name: Confirm end of fake run
        run: echo "Finished fake 10-minute job for pricing test"
